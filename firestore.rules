rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // SortIt gebruikt Realtime Database. Houd de ongebruikte Firestore-database
    // volledig gesloten voor mobiele en webclients.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
